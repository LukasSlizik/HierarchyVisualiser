<UserControl x:Class="HierarchyVisualiser.Views.ClassView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:behaviors="clr-namespace:HierarchyVisualiser.Behaviors"
             xmlns:local="clr-namespace:HierarchyVisualiser"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             behaviors:DragAndDropBehavior.CanBeDragged="True">

    <UserControl.Resources>
        <ResourceDictionary>
            <DataTemplate x:Key="ClassMemberTemplate">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Image Source="..\Icons\method.png" Grid.Column="0"/>
                    <Image Source="..\Icons\property.jpg" Grid.Column="0"/>
                    <TextBlock Text="{Binding Name}" Grid.Column="1"/>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border BorderBrush="Black" BorderThickness="2" CornerRadius="5">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="{Binding ClassName}" Margin="5"  />
            <ListBox Grid.Row="1" ItemsSource="{Binding Properties}" BorderThickness="0 1 0 0" Margin="5" ItemTemplate="{StaticResource ClassMemberTemplate}"  />
            <ListBox Grid.Row="2" ItemsSource="{Binding Methods}" BorderThickness="0 1 0 0" Margin="5" ItemTemplate="{StaticResource ClassMemberTemplate}" />
        </Grid>
    </Border>

</UserControl>
